// weird request from HR
// pass this challenge
let firstInput = 2;
let secondInput = [[1, 0, 5], [1, 1, 7], [1, 0, 3], [2, 1, 0], [2, 1, 1]];
let secondInputLength = secondInput.length; // 5
let lastAnswer = 0;

console.log(secondInputLength);

function dynamicArray(n, queries) {
  // Write your code here
  console.log(n);
  console.log(queries);
}

dynamicArray(firstInput, secondInput);
